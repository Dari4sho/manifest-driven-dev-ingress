# Internal frontend listen port (inside each project network)
FRONTEND_PORT=3000

# Frontend build/runtime API origin
# Default generated by bin/dev-up:
# API_BASE_URL=http://api-<DEV_SLUG>.localhost[:TRAEFIK_HTTP_PORT]
VITE_API_BASE_URL=
